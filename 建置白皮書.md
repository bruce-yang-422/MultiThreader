# MultiThreader 多脆客｜基礎版建置白皮書

版本：v0.5｜日期：2026-09-22｜狀態：第一階段程式已建立，本機模擬驗證通過，真實 API 待驗收

目前交付：獨立 Flask／SQLite 程式、繁體中文介面、兩種圖文批次發布、OAuth 與加密憑證保存、草稿、權限、工作程序、失敗重試與逾時查核。單篇支援文字及一張圖片。模擬模式與正式模式資料庫分開。

操作見 [README.md](README.md)，Meta App、Cloudinary 與 HTTPS 設定見 [設定指南.md](設定指南.md)。使用者已完成 Meta App、Threads API、測試者 petstar_5566 邀請接受及基本／發文權限設定。本機已切換 live 模式，固定回呼網址與 OAuth 導向已檢查；下一步由 petstar_5566 完成首次 OAuth，再驗收真實發文。圖片託管尚待設定，不能以模擬結果代替實際平台驗收。

## 1. 目的與建置方向

提供公司非 IT 同事與社群編輯使用的網頁工具，集中管理 **Threads、Facebook 粉絲專頁、Instagram**，完成內容編輯、立即發布與自動排程。

採用 **Python / Flask + HTML / Tailwind CSS**。建議以 [Insights](https://github.com/l0lsec/Insights) 為基底試跑、驗證後改版，保留帳號與發布能力，製作繁體中文介面。沿用程式時保留原專案 MIT 授權聲明；上游現有功能不等於已通過本案驗收。

**第一版完成 Threads 多帳號圖文發布：同一篇發往多帳號，以及不同篇分別指定帳號，皆可一次提交發布。帳號首次逐一授權後，日常直接選取使用。** Facebook、Instagram、排程與背景內容處理保留為後續階段的既定需求，不阻擋第一版交付。

Insights 已下載至 `D:\Tools\MultiThreader\Insights` 作為參考庫，版本為 `b682ef5`；多脆客的實作另行建立，保留參考庫原貌。

## 2. 第一版與後續功能

### 第一版：Threads 多帳號發文

- 管理者逐一完成帳號 OAuth 授權，核對名稱、頭像與 Threads user ID，避免瀏覽器登入狀態導致連錯帳號。
- 每個帳號獨立保存加密 access token、到期時間與連線狀態；同一 user ID 再次授權時更新原帳號，不新增重複項目。
- 同事登入多脆客後，撰寫、預覽文字／圖片貼文並指定目標帳號；支援下列兩種模式，畫面清楚顯示每篇內容與帳號對應。
- 由後端依規則更新可更新的 token；關閉瀏覽器或重啟服務後仍保留帳號連結。
- 授權過期、撤銷或被平台判定失效時，只要求該帳號重新連結，其餘帳號可繼續使用。
- 提供草稿、發布結果與貼文連結，保留失敗內容並避免重複發文。

| 發布模式 | 範例 | 操作 |
| --- | --- | --- |
| 同篇多帳號 | 圖文甲 → A、B、C | 編輯一篇圖文，勾選多個帳號，一次確認發布 |
| 不同篇指定帳號 | 圖文甲 → A；圖文乙 → B | 新增多張圖文卡片，各自選擇帳號，一次確認整批發布 |

「同步發」定義為一次操作啟動同批發布，各帳號分別送出，實際完成時間可有先後，不保證同一秒上線。

每個「圖文＋目標帳號」獨立記錄進度、結果及貼文連結。某帳號失敗不阻擋其他帳號；只對已確認失敗的項目提供重試，成功項目不重發，逾時待確認項目先查核。同一批次重複提交不得建立重複工作。提交時保存文案、圖片與帳號的快照，之後修改草稿不影響已送出的工作。

本工具不保存 Threads 密碼。首次授權可能需要在官方頁面登入或切換帳號；完成後使用保存的授權發文，不能承諾永久免重新授權。多脆客內部登入與社群授權分開，內部登入到期不代表所有社群帳號都需重新授權。

### 完整需求路線

| 項目 | 規劃內容（依下方階段交付） |
| --- | --- |
| 登入與帳號管理 | 同事各自登入；管理者連結多個社群帳號、命名、指派操作權限、重新授權及解除連結 |
| Threads | OAuth 授權，發布文字、文字搭配圖片 |
| Facebook | OAuth 授權，選擇有管理權限的粉絲專頁，發布文字與圖片 |
| Instagram | OAuth 授權，發布動態貼文、輪播（本版設定 2–10 張圖片／影片）、Reels 與快拍（圖片／影片）；依帳號資格開放 |
| 草稿與預覽 | 上傳素材、儲存草稿、檢查各平台格式，發布前確認帳號與內容 |
| 時段管理 | 按星期、平台設定重複時段，例如週一至週五 09:00、18:00 |
| 自動排隊 | 貼文自動安排至目標帳號下一個可用時段，顯示確切發布時間 |
| 每日限額 | 設定每個平台每日發文上限，可另設帳號上限 |
| 背景工作 | 自動發布、素材處理、授權狀態檢查；保留資訊流刷新與劇集處理功能 |
| 發文紀錄 | 顯示操作者、平台、帳號、排程時間、發布結果與可取得的貼文連結 |

背景資訊流刷新指 RSS 等來源更新；劇集處理指 Podcast／影音單集的下載、轉錄等工作。兩者納入需求，預設停用，由管理者設定來源、服務及必要金鑰後啟用，失敗不得阻塞排程發文。完整 AI 文案工作台、審稿及成效分析列為後續擴充。

以上平台與排程能力由 [Insights 專案說明](https://github.com/l0lsec/Insights#readme) 列出；本案仍須逐項測試。

## 3. 帳號條件與授權

| 平台 | 需要準備 | 後端保存 |
| --- | --- | --- |
| Threads | 可完成官方 OAuth 授權的 Threads 帳號 | Threads user ID、該帳號的 access token |
| Facebook | 具粉絲專頁發布權限的 Facebook 使用者 | Page ID、對應 Page access token |
| Instagram | 專業帳號：企業／商業（Business）或創作者（Creator） | Instagram 帳號 ID、對應 access token |

Instagram 的「專業帳號」是帳號類型，不是本工具的付費版本。一般個人帳號需先切換為符合 API 要求的專業帳號。Insights 採用 Instagram Login；此官方流程不要求綁定 Facebook 粉絲專頁。依據：[Meta Instagram Login 說明](https://www.postman.com/meta/instagram/folder/6raa77c/instagram-api-with-instagram-login)。

快拍功能須另確認資格：Meta 的 Facebook Login 文件明列快拍發布限 Business 帳號；本案採 Instagram Login，須核對該路徑當期規則並實測，不預先承諾所有 Creator 帳號都可用。需要完整快拍功能的公司帳號，建議以 Business 帳號驗收。參考：[Meta Facebook Login 限制](https://www.postman.com/meta/instagram/folder/u4g5a2a/instagram-api-with-facebook-login)。

管理者設定各平台 App ID、App Secret、HTTPS 回呼網址及必要權限。同事透過官方授權頁連結帳號，不必手動貼 token。後端驗證 OAuth state，按「平台＋帳號 ID」保存加密憑證，依各平台規則更新；失效時提示重新連結。憑證不得出現在前端、日誌或 Git。

## 4. 編輯操作流程

第一版日常操作：

1. 登入多脆客，選擇「同篇多帳號」或「不同篇指定帳號」。
2. 編輯圖文卡片並勾選目標帳號；不同篇模式可新增卡片，各自設定文字、圖片與帳號。
3. 預覽整批對應清單，例如「甲 → A／B／C」或「甲 → A、乙 → B」，確認內容與發布份數。
4. 按下「確認整批發布」，逐項顯示處理中、成功、失敗或待確認及貼文連結。
5. 有失敗時保留圖文，只重試已確認失敗的項目。

提交前檢查所有卡片的內容、素材及帳號操作權限。使用已授權帳號時，不重新要求輸入社群帳密。

後續擴充後的操作：

1. 登入，選擇平台與已獲授權的帳號。
2. 選貼文類型，輸入文案、上傳圖片或影片。
3. 預覽並修正格式；超限時提示修改，不直接截斷文案。
4. 選擇「立即發布」「指定時間」或「加入自動排隊」。
5. 確認帳號、內容及時間，在排程列表查看結果、調整或取消尚未開始的工作。

介面採繁體中文，主要頁面為「帳號管理」「撰寫貼文」「排程佇列」「發文紀錄」。管理者另有「時段與限額」「背景工作」設定。

## 5. 排程與背景執行規則

- 預設時區為 Asia/Taipei，畫面顯示時區，資料庫以 UTC 保存時間。
- 平台時段作為範本，各帳號獨立排隊；同帳號同一時段只安排一篇。
- 重複時段代表每週可用的發布位置，不會自動重複發布相同內容。
- 每日平台上限按公司內該平台所有帳號合計；排程須預留名額，即時發文也計入。滿額時排至下一個可用日期；官方 API 配額另行檢查。
- 限額或時段變更時標示受影響工作，供編輯確認重排；不得默默更動既有承諾時間。
- 工作狀態包含：待發布、處理中、成功、失敗、待確認、已取消。逾時先查核結果，不能直接重送。
- 排程保存於資料庫，獨立背景程序領取工作並鎖定，避免多個程序重複發布；重啟後恢復待辦。
- 停機錯過的排程標記待處理，由編輯選擇補發或重排。停用帳號或授權失效時暫停該帳號工作。
- RSS 刷新及劇集處理與發布工作隔離，提供啟停、進度與失敗紀錄；只處理已設定的來源。

上述為本案目標行為，須檢查並補足 Insights 現有實作。排程主機必須保持運行，關閉瀏覽器不影響背景工作。

## 6. 技術架構

```text
瀏覽器：HTML / Tailwind CSS
              ↓ HTTPS
Python / Flask：登入、OAuth、草稿、素材、排程介面
              ↓
資料庫：帳號、權限、貼文、時段、工作與發布紀錄
              ↑
背景程序：領取工作 → 檢查限額與授權 → 各平台 API
素材儲存：提供 Meta 可讀取的 HTTPS 圖片／影片網址
```

小團隊單機試用採 SQLite，控制寫入併發；擴大使用再評估 PostgreSQL。素材可使用 Cloudinary 或相容物件儲存，排程執行時網址仍須有效；後端按平台檢查格式、大小及影片處理狀態。

正式部署配置網域、HTTPS、正式 WSGI 伺服器、背景程序自動啟動與監控、資料備份。先確認 Insights 的登入與權限、憑證保存、排程鎖定及素材流程符合需求，再交付同事。

## 7. 建置與驗收順序

| 階段 | 交付與驗收 |
| --- | --- |
| 1. 第一版：Threads | 參考 Insights 建立多脆客；完成 OAuth、多帳號憑證保存、同篇多帳號發布、不同篇指定帳號批次發布、逐項結果與失敗重試 |
| 2. 平台擴充 | 加入 Facebook 粉絲專頁及合資格 IG 帳號，逐項測試文字、圖片及 IG 各媒體類型 |
| 3. 排程功能 | 驗證星期時段、自動分配、每日限額、台北時區、調整／取消、跨日排程及重啟恢復 |
| 4. 背景工作 | 驗證 RSS 刷新與一筆劇集處理，確認不影響定時發布；授權失效或 API 逾時可追查且不重複發文 |
| 5. 同事試用 | 完成繁體中文介面、權限限制、HTTPS、備份及一頁操作指南，讓編輯自行完成發文和排程 |

第一版獨立驗收條件：

1. 至少三個不同 Threads 帳號 A／B／C 成功授權，ID 與憑證對應正確。
2. 同篇模式：圖文甲一次提交至 A／B／C，三個帳號各有一篇正確圖文，逐項顯示結果與連結。
3. 不同篇模式：同批提交圖文甲 → A、圖文乙 → B；A 只有甲、B 只有乙，文字與圖片均不混用。
4. 兩種模式及後續切換均不要求再次輸入 Threads 帳密；關閉瀏覽器、重新登入多脆客及重啟服務後，有效授權仍可使用。
5. A／C 成功、B 失敗時，整批顯示部分成功；只重試 B，A／C 不重發。逾時項目顯示待確認，查核前不直接重送。
6. 重複點擊或重送同一批次不重複發文；未獲指派的同事無法發布至該帳號。
7. token 更新成功後保存新憑證；單一帳號失效可重新連結且不影響其他帳號。提交後修改草稿不改變已送出的內容。

第一版交付即包含繁體中文基本介面、HTTPS、憑證保護與簡短操作指南；後續階段擴充同一套介面。一般同事正式授權前，完成該階段平台要求的權限審查／驗證；外部審查時間另列，不視為程式完成即可上線。

## 8. 參考與查核範圍

- [Insights 專案](https://github.com/l0lsec/Insights)
- [Threads 官方文件](https://developers.facebook.com/documentation/threads)
- [Meta Threads API 範例](https://www.postman.com/meta/threads/documentation/dht3nzz/threads-api)
- [Meta Instagram API 文件](https://www.postman.com/meta/instagram/documentation/6yqw8pt/instagram-api)

本版依專案文件、部分程式及 Meta 官方 Postman 文件實作。Insights 作為未修改的參考庫，多脆客已獨立安裝依賴並完成模擬測試。Meta 開發者網站部分頁面無法直接讀取；正式串接時再次核對權限、媒體限制、帳號資格與 API 版本。
