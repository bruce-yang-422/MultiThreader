# MultiThreader｜一鍵開關控制台規劃

日期：2026-09-22｜狀態：規劃，待 Threads OAuth 與首次發文驗收後實作

## 1. 目標與方案

讓非 IT 同事雙擊桌面圖示，就能啟動、停止、重啟 Cloudflare Tunnel 與多脆客，無須輸入指令或操作服務管理員。

採用 **PowerShell + Windows Forms 圖形介面**，利用 Windows 的 .NET 元件，不另建 C# 專案或網頁控制服務。控制台與網頁服務分開執行，網站停止時仍能使用控制台。

本次只建立規劃，不修改 `Start.ps1`、`Stop.ps1` 或執行中的服務。

## 2. 同事看到的畫面

```text
┌──────────────────────────────────────────┐
│         MultiThreader 多脆客 控制台        │
├──────────────────────────────────────────┤
│ 網路入口（Cloudflare）        ● 已連線     │
│ 多脆客                       ● 可使用     │
│   網頁                       已啟動       │
│   背景發文                   已啟動       │
│                                          │
│ [全部啟動]  [全部停止]  [全部重啟]        │
│                                          │
│ [開啟多脆客]  [查看問題紀錄]              │
│                                          │
│ multithreader.stack-base.com              │
│ 本機：127.0.0.1:18473                     │
│ 最近操作：全部啟動完成                    │
│ 關閉此視窗不會停止服務。                  │
└──────────────────────────────────────────┘
```

- 狀態用文字與顏色共同表示：已停止、啟動中、可使用、部分異常、停止中。
- 操作期間顯示進度並停用衝突按鈕，避免連點重複啟動。
- 視窗關閉只關控制台；要停服務，按「全部停止」。
- 操作訊息採中文，例如「網頁已啟動，背景發文尚未就緒」。

## 3. 按鈕行為

| 按鈕 | 預期動作 |
| --- | --- |
| 全部啟動 | 檢查設定 → 啟動／確認 Cloudflared → 啟動網頁 → 啟動 worker → 檢查本機及公開入口 |
| 全部停止 | 暫停接受新發布與領取新工作 → 等待正在發布的項目完成 → 停止 worker、網頁 → 停止本工具專用 Tunnel |
| 全部重啟 | 完成安全停止，再執行啟動；已保存的草稿、token 與工作保留 |
| 開啟多脆客 | 用預設瀏覽器開啟 https://multithreader.stack-base.com |
| 查看問題紀錄 | 開啟本次操作摘要；詳細日誌另有入口，不顯示 token、密碼或 App Secret |

若目前有發文，顯示「正在完成 N 個發布項目，請稍候」。等待超過設定時間時顯示未完成，保留程序並允許取消停止；一般按鈕不直接強制結束發布程序。待發布工作保留在資料庫，下次啟動再處理。

## 4. 狀態判斷

**不能只看到 18473 有程序就顯示正常。**

| 項目 | 檢查方式 |
| --- | --- |
| Cloudflared 服務 | Windows Service 狀態；Running 只代表程序運行，不代表 Tunnel 已連通 |
| 本機網頁 | 檢查本工具的程序身分及本機健康檢查回應 |
| 背景發文 | 檢查 worker 程序與持續心跳；需補足耗時工作期間的獨立心跳 |
| 公開入口 | 檢查固定 HTTPS 網域的健康回應，識別確實是 MultiThreader |
| 整體可使用 | 網頁、worker 與公開入口均正常才顯示綠色 |

預計每 3 秒非同步更新本機狀態，公開入口每 15 秒檢查一次。檢查設逾時，網路中斷時視窗仍可操作。健康回應只提供必要狀態，不公開帳號與設定內容。

## 5. 權限與首次設定

管理者只需進行一次安裝設定：確認 Python 環境、`.env`、多脆客內部管理者、Cloudflared 服務及固定網域，建立桌面捷徑。

- 目前本機已有 `Cloudflared` Windows Service，查核時為 Running／Auto；實作前需確認它連接的是 MultiThreader Tunnel，且是否與其他網站共用。
- 預期 Tunnel ID：`f0c5c608-6c46-4903-b75b-7f9fb3f4dd4e`；固定主機名：`multithreader.stack-base.com`。
- 若服務承載其他網站，不能將「全部停止」直接綁到停止共用服務。需先配置專用 connector／服務，或明確將共用入口排除出可停止範圍。
- 控制台、Flask、worker 以一般使用者身分執行。Cloudflared 啟停需要的權限獨立處理。
- 若管理者預先授予指定 Windows 使用者群組該服務的查詢、啟動及停止權限，同事日常可直接操作；否則控制服務時會出現 Windows UAC，標準帳號可能需要管理者協助。
- 權限不足時顯示「需要管理者完成首次設定」，不要求同事自行修改系統權限。

Windows 服務控制的權限依實際服務 ACL 而定，不能只做桌面捷徑就承諾完全免管理者。參考：[Microsoft Start-Service 文件](https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.management/start-service)。

## 6. 現有腳本需補足的地方（之後實作）

目前 `Start.ps1` 會呼叫互動式初始化，網頁在前景執行，也未同步寫入 `Stop.ps1` 使用的程序清單。因此不能直接加幾個 GUI 按鈕就視為可靠完成。

1. 將首次建立管理者與日常啟動分開；一般同事按啟動時不出現帳密輸入終端。
2. 建立共用的啟停模組，供 GUI 與 `Start.ps1`／`Stop.ps1` 使用，統一 PID、啟動時間、執行路徑及狀態紀錄。
3. 重複按啟動只補起缺少的程序；連接埠被其他軟體佔用時說明原因，不終止對方程序。
4. 處理 Windows venv 的父／子 Python 程序，核對程序身分後再停止，避免 PID 重用誤停其他軟體。
5. 補上網頁健康檢查、worker 心跳與安全停止訊號；停止期間禁止新工作進入發布步驟。
6. GUI 操作在背景執行、正常啟動不閃黑色命令視窗；取消 UAC 或啟動失敗均顯示真實狀態。
7. 日誌輪替並記錄每次操作結果；不得包含社群憑證或完整敏感請求網址。
8. 固定網域直接沿用現有 Tunnel，不啟動 Quick Tunnel，也不改寫 OAuth 回呼網址。

## 7. 預計交付

```text
MultiThreader/
├─ ControlPanel.ps1           圖形控制台
├─ ServiceControl.psm1        共用啟停與狀態檢查
├─ Install-ControlPanel.ps1   一次性檢查與建立捷徑
├─ Start.ps1 / Stop.ps1       改用共用模組
└─ instance/control.log       操作紀錄

桌面／MultiThreader 控制台.lnk
```

第一版不加入開機自動發布、排程服務安裝或系統匣常駐；控制台管理目前的網頁、worker 與專用 Cloudflared 服務即可。

## 8. 驗收與時機

先完成 petstar_5566 OAuth 與一次真實發文，再實作控制台。

驗收：同事只用桌面圖示即可啟停；重複點擊不建立重複程序；網頁正常但 worker 停止時能顯示異常；公開入口中斷不誤報成功；關閉視窗不影響服務；發文中停止不直接強制結束；重新啟動保留授權、草稿與待發布工作，未知發布結果仍先查核。
