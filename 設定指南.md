# 多脆客｜Meta Threads 與 HTTPS 設定

目前使用固定入口 **https://multithreader.stack-base.com**，本機轉送為 `http://127.0.0.1:18473`。
`.env` 的回呼網址已設為 **https://multithreader.stack-base.com/threads/callback**；Meta 後台的有效重新導向 URI 也需填入相同值。固定 Tunnel 使用期間不需啟動下方臨時入口工具。

## 1. 臨時 HTTPS 入口

已提供 Cloudflare Quick Tunnel 啟動工具；首次下載會核對官方 release 的 SHA256。

```powershell
cd D:\Tools\MultiThreader
.\.venv\Scripts\python.exe tools/start_https.py
```

工具會顯示 HTTPS 首頁與 `/threads/callback` 回呼網址，並更新 `.env` 的 `THREADS_REDIRECT_URI`、`COOKIE_SECURE=true`。網址另存在 `instance/https-url.txt`。啟動後重啟 `app.py`，從同一個 HTTPS 網址登入並開始 OAuth，避免本機與外部網址的 session 不一致。

保持 tunnel、app、worker 三個程序運行。臨時網址重開可能改變：需同步更新 Meta 後台回呼網址並重啟網頁／worker。原本已保存的有效 token 不會因程式重啟而刪除。Quick Tunnel 僅供測試，正式使用改成固定網域與持續運行的 tunnel／主機。依據：[Cloudflare Quick Tunnels](https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/do-more-with-tunnels/trycloudflare/)。

若只想回到本機 HTTP 試用，將 `.env` 的 `COOKIE_SECURE=false`，重啟後開啟 http://127.0.0.1:18473 。

## 2. 建立 Meta App（官方後台操作）

1. 使用有權管理公司 App 的 Facebook／Meta 開發者身分，登入 [Meta 開發者後台](https://developers.facebook.com/apps/)。若尚未註冊，先完成官方要求的開發者註冊與驗證。
2. 建立 App，名稱可用 `MultiThreader 多脆客`，選擇提供 Threads API 的使用案例。選項名稱可能調整，以當時後台為準。
3. 在 Threads 使用案例設定中取得 **Threads App ID 與 Threads App Secret**，不要拿 Threads 帳號密碼代替。
4. 在 OAuth 有效重新導向 URI 填入第 1 步產生的完整網址，例如 `https://xxxxx.trycloudflare.com/threads/callback`，路徑、HTTPS 與結尾都必須一致。
5. 開發模式先設定測試者角色／邀請，用指定帳號完成平台要求的邀請接受步驟。先用翔志管理的一個 Threads 帳號測試，再逐一加入其他測試帳號。
6. 本工具申請 `threads_basic` 與 `threads_content_publish`。供非測試角色使用前，依後台要求完成權限進階存取、App Review 或驗證。

App 的擁有者身分與被授權發文的 Threads 帳號可能不同；四組社群帳密本身不能代替開發者 App 設定。官方登入／驗證及帳號授權需由有權限的人在瀏覽器完成，本程式不以密碼自動登入 Threads。

官方依據：[Threads 文件](https://developers.facebook.com/documentation/threads)、[Meta 官方 API 範例](https://www.postman.com/meta/threads/documentation/dht3nzz/threads-api)。使用者已完成 Meta App、Threads API、測試者 petstar_5566 的邀請接受及必要權限設定。目前 Flask 與 worker 已切換 live，App 設定及回呼導向已確認；尚待首次真實 OAuth。

## 3. 填入本機設定並切換正式模式

用編輯器開啟 `.env`，保留工具已填入的 HTTPS 回呼網址，填入：

```dotenv
THREADS_MODE=live
THREADS_APP_ID=你的ThreadsAppID
THREADS_APP_SECRET=你的ThreadsAppSecret
THREADS_REDIRECT_URI=https://你的臨時網址/threads/callback
COOKIE_SECURE=true
```

不要把 App Secret 或社群密碼貼到公開訊息或提交 Git。程序只在啟動時讀取 `.env`，修改後要重啟。

正式模式使用獨立資料庫，首次先執行 `python manage.py init` 建立管理者，再啟動 `python app.py` 與 `python worker.py`。

## 4. 授權翔志管理的測試帳號

1. 從 HTTPS 網址登入多脆客，開啟「帳號管理」→「連結 Threads 帳號」。
2. 在 Meta 官方授權頁登入指定測試帳號、同意授權。
3. 回到多脆客核對頭像、使用者名稱與 Threads user ID，再按「確認連結」。
4. 編輯帳號別名。若之後由其他同事發文，在下方建立內部編輯帳號並指派權限。
5. 連結下一個 Threads 帳號時，確認官方頁面目前登入的是誰；必要時在官方頁面切換或使用另一個瀏覽器設定檔。若使用另一個設定檔，須先在該設定檔登入同一 HTTPS 多脆客，再從「連結」開始。

同一 Threads user ID 再次授權會更新原帳號，不會新增重複帳號。只有管理者可連結社群帳號，編輯只能發布到已獲指派的帳號。

## 5. 圖片託管

第一版使用 Cloudinary 將上傳圖片轉為 Meta 可讀取的 HTTPS 圖片。建立／使用自己的 Cloudinary 帳戶後，將下列值填入 `.env` 並重啟：

```dotenv
CLOUDINARY_CLOUD_NAME=你的cloud_name
CLOUDINARY_API_KEY=你的api_key
CLOUDINARY_API_SECRET=你的api_secret
```

上傳限制為 8 MB、2500 萬像素以內的 JPG／PNG／WebP；伺服器去除原始 EXIF、轉成 JPEG，最長邊縮至 1920。每張卡片一張圖片。未設定託管時，正式模式可先測純文字。

## 6. 真實驗收順序

1. 先授權翔志管理的一個帳號，人工確認一篇測試文字後發布，核對 Threads 上的結果。
2. 設定圖片託管，發布一篇圖文並核對。
3. 依序連結至少三個合資格帳號 A／B／C。
4. 同篇甲 → A／B／C；不同篇甲 → A、乙 → B；逐一查看結果與連結。
5. 重新登入、重啟 app／worker，確認無須重新輸入社群帳密。

測試文需使用可公開的內容，且由你在預覽頁確認發布。模擬測試通過不代表已通過 Meta 真實 API 驗收。

## 常見狀況

| 狀況 | 處理 |
| --- | --- |
| OAuth 回呼網址不符 | 比對 `.env` 與 Meta 後台，臨時 tunnel 重開後通常需重填 |
| state 驗證失敗／登入失效 | 從同一 HTTPS 網址重新登入，再開始連結，不要混用 localhost |
| 帳號沒有權限 | 確認測試者邀請已接受、App 使用案例與要求權限已設定 |
| 全部停在待發布 | 啟動 worker，查看 `instance/worker.error.log` |
| 待確認不能重試 | 先查核結果；仍不確定時至 Threads 核對，勿整批重送 |
| 換主機後無法解密 | 還原原加密金鑰，或重新授權；不要重建金鑰後覆蓋舊備份 |


## 圖片與影片（本機素材）

現在上傳素材儲存在 `instance/media/`，透過現有 HTTPS 網域的 `/media/<隨機識別碼>` 提供 Meta 下載，不必設定 Cloudinary。正式模式的素材網址免登入，有網址的人即可讀取；網址使用隨機 UUID，不提供目錄清單。請勿上傳不打算公開的素材。舊 Cloudinary 圖片仍可沿用。

- 圖片：JPG、PNG、WebP，最多 8 MB、2500 萬像素；轉 JPEG，透明背景轉白色。
- 影片：MP4、MOV，最多 90 MB、5 分鐘；H.264／H.265 視訊、AAC 音訊。需安裝 FFmpeg 並讓 `ffprobe` 在 PATH 中。這是工具目前限制；平台仍會驗證素材。
- 每篇可放 1–20 張圖片或一支影片，支援同篇多帳號及不同篇指定帳號。
- HTTPS 主機取自 `THREADS_REDIRECT_URI`；需要獨立素材網域時設定 `MEDIA_BASE_URL=https://你的素材網域`，其 `/media/` 需指向本程式。
- 發布處理完成前需保持電腦、程式與 Tunnel 開啟；Cloudflare 不可要求 Meta 登入或通過互動驗證。
- 刪除發文紀錄不會刪除素材檔案；目前不自動清理，以保留其他草稿與發布使用中的素材。


## 多張圖片與第一則回覆

選檔可一次選多張，也可分次加入，最多 20 張，每張最多 8 MB。「往前」調整順序，「移除」刪除選取。2 張以上以 Threads 輪播發布；影片仍限單支，不與圖片混用。

「第一則回覆」為選填文字，最多 500 字元。每個目標帳號的主貼文成功後，由該帳號回覆自己的貼文；不保證平台排序或早於其他人留言。可在不同篇模式為各帳號填不同內容。

主貼文與回覆分別保存結果。回覆失敗可「只重試第一則回覆」，不會重發主貼文；待確認先查核，不自動重送。主貼文失敗時不送回覆。清空歷史會保留仍待發布、處理中、待確認的回覆紀錄。

API 依據：[Meta 官方 Threads API 範例（輪播 2–20 張、reply_to_id）](https://www.postman.com/meta/threads/documentation/dht3nzz/threads-api)。

首次啟用回覆：在 Meta App 加入 `threads_manage_replies`，到「Threads 發文帳號」按各帳號的「授權第一則回覆」完成授權。一般連結入口仍只要求原有發文權限；回覆權限由專用入口加授權。正式 Meta 發布仍需實際驗證。
